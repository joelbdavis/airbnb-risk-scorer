# Hospitable API key (get from Hospitable dashboard)
HOSPITABLE_API_KEY=your-api-key-here

# Port for Express server (optional fallback)
PORT=3000

# ngrok Auth Token (if using ngrok in dev)
NGROK_AUTHTOKEN=your-ngrok-authtoken-here
